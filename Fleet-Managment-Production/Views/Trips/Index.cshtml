@model IEnumerable<Fleet_Managment_Production.Models.Trip>
@using Microsoft.AspNetCore.Identity
@using Fleet_Managment_Production.Models
@inject UserManager<Users> UserManager

@{
    var currentUserId = UserManager.GetUserId(User);
}

<div class="container-fluid py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h3 class="fw-bold mb-1">Rejestr Tras</h3>
            <p class="text-muted mb-0">Historia przejazdów, ewidencja przebiegu i logistyka.</p>
        </div>
        <a asp-action="Create" class="btn btn-primary shadow-sm">
            <i class="bi bi-plus-lg me-2"></i>Nowa Trasa
        </a>
    </div>

    <div class="card shadow-sm border-0 mb-3">
        <div class="card-body p-3">
            <form asp-action="Index" method="get" class="row align-items-end">
                <div class="col-md-3">
                    <label class="form-label text-muted fw-semibold mb-1" style="font-size: 0.85rem;">Filtruj po kierowcy:</label>
                    <select name="filterDriverId" class="form-select shadow-sm" asp-items="@(ViewData["DriversList"] as SelectList)">
                        <option value="">Wszyscy kierowcy</option>
                    </select>
                </div>
                <div class="col-md-3 mt-3 mt-md-0">
                    <label class="form-label text-muted fw-semibold mb-1" style="font-size: 0.85rem;">Filtruj po pojeździe:</label>
                    <select name="filterVehicleId" class="form-select shadow-sm" asp-items="@(ViewData["VehiclesList"] as SelectList)">
                        <option value="">Wszystkie pojazdy</option>
                    </select>
                </div>
                <div class="col-md-2 mt-3 mt-md-0">
                    <button type="submit" class="btn btn-secondary shadow-sm w-100"><i class="bi bi-funnel me-2"></i>Filtruj</button>
                </div>
                <div class="col-md-2 mt-2 mt-md-0">
                    <a asp-action="Index" class="btn btn-outline-secondary shadow-sm w-100">Wyczyść</a>
                </div>
            </form>
        </div>
    </div>
    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4 py-3">Data</th>
                            <th>Trasa (Start &rarr; Cel)</th>
                            <th>Pojazd / Kierowca</th>
                            <th>Dystans</th>
                            <th>Typ</th>
                            <th class="text-end pe-4">Akcje</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="6" class="text-center py-4 text-muted">Brak zarejestrowanych tras pasujących do kryteriów.</td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model)
                            {
                                var canEdit = User.IsInRole("Admin") || User.IsInRole("Manager") ||
                                (item.Driver != null && item.Driver.UserId == currentUserId);

                                <tr>
                                    <td class="ps-4">
                                        <div class="d-flex flex-column">
                                            <span class="fw-bold text-dark">@item.StartDate.ToShortDateString()</span>
                                            <span class="small text-muted"><i class="bi bi-clock me-1"></i>@item.StartDate.ToShortTimeString()</span>
                                        </div>
                                    </td>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="bg-light rounded p-1 border me-2">
                                                <i class="bi bi-geo-alt text-primary"></i>
                                            </div>
                                            <div>
                                                <span class="fw-bold text-dark">@item.StartLocation</span>
                                                <i class="bi bi-arrow-right mx-2 text-muted small"></i>
                                                <span class="fw-bold text-dark">@item.EndLocation</span>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        @if (item.Vehicle != null)
                                        {
                                            <div class="badge bg-white text-dark border mb-1">
                                                <i class="bi bi-car-front-fill me-1 text-secondary"></i>@item.Vehicle.LicensePlate
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="badge bg-danger bg-opacity-10 text-danger border border-danger mb-1">Brak auta</div>
                                        }

                                        <div class="small text-muted">
                                            <i class="bi bi-person me-1"></i>
                                            @(item.Driver != null ? $"{item.Driver.FirstName} {item.Driver.LastName}" : "Brak kierowcy")
                                        </div>
                                    </td>
                                    <td>
                                        <span class="fw-bold">@item.EstimatedDistanceKm</span> <span class="text-muted small">km</span>
                                    </td>
                                    <td>
                                        @if (item.TripType == Fleet_Managment_Production.Models.TripType.Business)
                                        {
                                            <span class="badge bg-primary bg-opacity-10 text-primary border border-primary rounded-pill px-3">Służbowa</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary bg-opacity-10 text-secondary border border-secondary rounded-pill px-3">Prywatna</span>
                                        }
                                    </td>
                                    <td class="text-end pe-4">
                                        <div class="btn-group shadow-sm">
                                            <a asp-action="Details" asp-route-id="@item.Id" class="btn-table-action" title="Szczegóły">
                                                <i class="bi bi-eye"></i>
                                            </a>

                                            @if (canEdit)
                                            {
                                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn-table-action" title="Edytuj">
                                                    <i class="bi bi-pencil"></i>
                                                </a>
                                            }

                                            @if (User.IsInRole("Admin"))
                                            {
                                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline">
                                                    <button type="button" class="btn-table-action btn-delete-swal" title="Usuń">
                                                        <i class="bi bi-trash"></i>
                                                    </button>
                                                </form>
                                            }
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    @if (ViewBag.TotalPages > 1)
    {
        <nav aria-label="Nawigacja stronami" class="mt-4 pb-4">
            <ul class="pagination justify-content-center flex-wrap shadow-sm mb-0">
                <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="Index"
                       asp-route-page="@(ViewBag.CurrentPage - 1)"
                       asp-route-filterDriverId="@ViewData["CurrentDriverFilter"]"
                       asp-route-filterVehicleId="@ViewData["CurrentVehicleFilter"]">
                        <i class="bi bi-chevron-left"></i>
                    </a>
                </li>

                @for (int i = 1; i <= ViewBag.TotalPages; i++)
                {
                    <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="Index"
                           asp-route-page="@i"
                           asp-route-filterDriverId="@ViewData["CurrentDriverFilter"]"
                           asp-route-filterVehicleId="@ViewData["CurrentVehicleFilter"]">@i</a>
                    </li>
                }

                <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="Index"
                       asp-route-page="@(ViewBag.CurrentPage + 1)"
                       asp-route-filterDriverId="@ViewData["CurrentDriverFilter"]"
                       asp-route-filterVehicleId="@ViewData["CurrentVehicleFilter"]">
                        <i class="bi bi-chevron-right"></i>
                    </a>
                </li>
            </ul>
        </nav>
    }
</div>